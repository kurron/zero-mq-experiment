cloud:
    aws:
        credentials:
            accessKey: FAKEKEY
            secretKey: FAKEKEY
            instanceProfile: false
        region:
            auto: false
            static: us-west-2

endpoints:
    enabled: true
    sensitive: false

spring:
    rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest